{
	"notes":[
		"JSON files always start with either an array [] or an objects {}",
		"JSON files cannot have comments, so put any comments in this array",
		"Commas go between items in an array or object, but there should never be trailing comma",
		"All property names must be inside quotation marks",
		"All string quotation mark must be double quotes",
		"Use a linter like jsonlint.com/ to check json"
	]
}